# Shared password that gates the whole docs site. Leave unset locally to run the
# site open; production fails closed (503) if it is unset.
DOCS_PASSWORD=

# Secret used to sign the session cookie. At least 32 characters, or the gate
# fails closed. Generate one with:
#   openssl rand -base64 48
DOCS_AUTH_SECRET=
